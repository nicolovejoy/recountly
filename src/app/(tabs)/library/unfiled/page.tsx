// Static segment — beats the [journalId] dynamic sibling, same precedence as
// api/entries/trash vs [id].

import UnfiledView from "../../../UnfiledView";

export default function UnfiledPage() {
  return <UnfiledView />;
}
